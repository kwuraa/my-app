import { Box } from "@chakra-ui/react";

// import { Btn } from "./Button";

// interface ICard {
//   event: () => void;
// }

// export const CardIndex = ({ event }: ICard) => {

export const CardIndex = ({ children }: any) => {
  return <Box>{children}</Box>;
};
